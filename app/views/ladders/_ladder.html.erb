<h1><%= ladder.name%></h1>

<li><%= link_to "Add Team", new_platform_game_ladder_team_path(@platform.id,@game.id, ladder.id)%> </li>

<% if user_signed_in? && is_admin?(current_user.email) %>
  <%= link_to "Destroy", platform_game_ladder_path(@platform, @game, ladder), method: :delete, data: {confirm: "Are you sure"} %>
<% end %>
