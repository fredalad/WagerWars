<h1><%= @platform.name%></h1>

<%= link_to "Destroy", platform_path(@platform), method: :delete, data: {confirm: "Are you sure?"} %>

<%= link_to "Add game", new_platform_game_path(@platform.id) %>
<%=render @platform.game %>
